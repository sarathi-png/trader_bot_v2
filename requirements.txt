pandas>=2.1.0
mplfinance>=0.12.10b0
ccxt>=4.2.0
requests>=2.31.0
python-dotenv>=1.0.0
numpy>=1.24.0
# Keep this in sync with `sdk_version` in README.md.
# gradio >= 5 is required: gradio 4.x imports the removed huggingface_hub.HfFolder
# and crashes with modern starlette/jinja2 ("TypeError: unhashable type: 'dict'").
gradio==6.15.1
yfinance>=1.0.0
